spring:
  application:
    name: remkey
#    connect to db
  datasource:
    url: jdbc:postgresql://localhost:5432/remkey
    username: admin
    password: Lovanky6408
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
#      config jpa for postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect


  flyway:
    enabled: true
    locations: classpath:db/migration
    repair-on-migrate: true
    baseline-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: lovankydev2002@gmail.com
    password: nlap sirq uxnr lsex
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


# json web token
jwt:
  secret-key: UoMpzIU1lwqnQ3qX6hHiJgtH0ZTr3P9UwtKua0c5sEZ
  valid-duration: 5 #day
  refreshable:  3 #day


app:
  sec:
    otp-verifier:
      valid-duration: 5 #minutes
      refreshable:  1 #minutes
      request-limit: 5 #times per day
    cors:
      endpoint: "*"

server:
  servlet:
    context-path: /api/v1/remkey
    port: 8080